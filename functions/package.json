{
  "name": "pmc-email-functions",
  "description": "PM Computers automated email notification service",
  "scripts": {
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions"
  },
  "engines": {
    "node": "18"
  },
  "dependencies": {
    "firebase-admin": "^11.8.0",
    "firebase-functions": "^4.3.1",
    "nodemailer": "^6.10.1",
    "resend": "^3.2.0"
  }
}
